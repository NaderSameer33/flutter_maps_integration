import 'package:flutter/material.dart';

class RootTrackItem extends StatelessWidget {
  const RootTrackItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}